{
  "name": "kilsaric-companion-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix angular-app run start",
    "build": "npm --prefix angular-app run build",
    "dev:backend": "npm --prefix backend run start:dev",
    "build:backend": "npm --prefix backend run build"
  }
}
